[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = [
  "assets",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests/python"]
python_files = ["test_*.py"]
addopts = "-q"

[tool.gdtoolkit]
line_length = 100
exclude = [
  "assets",
]
