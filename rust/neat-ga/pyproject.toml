[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "neat-ga"
version = "0.1.0"
description = "NEAT (NeuroEvolution of Augmenting Topologies) for chess neuroevolution"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
