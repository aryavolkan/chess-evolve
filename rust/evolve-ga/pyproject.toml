[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "evolve-ga"
version = "0.1.0"
description = "High-performance genetic algorithm operators for chess neuroevolution"
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
